name: Renovate
on:
  pull_request:
    paths:
      - '.github/renovate.json'
      - '.github/workflows/renovate.yml'
jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    container:
      image: renovate/renovate:latest@sha256:f2d4c467a8eb4b885630a8ca7d068173db69a5a1156ba41480c0a3a2e011d759
      options: --user root
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - id: validate
        run: renovate-config-validator > validation-result.txt
      - uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        if: ${{ always() }}
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const comments = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            const ids = comments.data
              .filter(e => e.user.id == 41898282)
              .map(e => e.id)
            await Promise.all(ids.map(async (id) => {
              github.rest.issues.deleteComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: id,
              })
            }))

            const fs = require("fs").promises
            const result = await fs.readFile('validation-result.txt', 'utf8')
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "## Renovate config validation result\n```\n" + result.trimEnd() + "\n```"
            })
